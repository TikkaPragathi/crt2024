#include <stdio.h>
#include<string.h>

int main() {
    char str[50];
    printf("Enter the string");
   gets(str);
   int n=strlen(str);
    printf("%s",str);
    return 0;
}
output:
Enter a string
java
java
