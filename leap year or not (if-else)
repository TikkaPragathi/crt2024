#include <stdio.h>

int main() {
   int a;
   scanf("%d",&a);
   if(a%4==0 && a%100!=0 || a%400==0)
   {
    printf("Leap year");
   }
 else
 {
     printf("Not a leap year");
 }
  
    return 0;
}

Output:
1929
Not a leap year

2024
Leap year(//divisible by 4 is a leap year)

2029(// 1929+100=2029)all century years are not a leap year
Not a leap year
